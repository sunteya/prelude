<div class="form-inputs">
	<%= f.input :email %>
	<%= f.input :login %>
	<%= f.input :password %>
	<%= f.input :password_confirmation %>
	<%= f.input :transfer_remaining %>
</div>

<div class="form-actions">
	<%= f.button :submit, class: "btn btn-primary"  %>
	<%= cancel_link(root_url) %>
</div>